\section{Limitations}
\label{sec:limitations}
% [WRITE NOW] No convergence claim; best-on-test probe metric (gap reported);
% model-selection inflation (smoothed); sparse 200-way kNN probe regime
% (relative signal only); single downstream task (CIFAR-10); stddev over
% training seeds, not data subsets (nested-prefix design); transfer of
% large-scale schedule findings open; cross-environment floor shift.
[TODO: Limitations --- drafted after the Discussion.]
